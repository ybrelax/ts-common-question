enum Gender {
    a = 1,
    b,
   // c = Gender.a,
    body = 'xx',
    girl = '1'
}

console.log(Gender.body);
console.log(Gender);

const enum Colors {
    Red,
    Yellow,
    Blue
}

console.log(Colors.Red, Colors.Yellow)